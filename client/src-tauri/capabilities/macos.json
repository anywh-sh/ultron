{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "macos-permissions",
  "description": "Checagem/pedido de permissão de microfone no macOS (voice.rs usa CoreAudio direto via cpal, que não dispara o diálogo nativo sozinho — ver client/src/lib/voice.ts).",
  "windows": ["*"],
  "platforms": ["macOS"],
  "permissions": ["macos-permissions:default"]
}
